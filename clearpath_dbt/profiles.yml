clearpath_dbt:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: clearpath
      schema: main
      schemas_and_paths:
        main: data/clearpath.db
      schema_directory: data